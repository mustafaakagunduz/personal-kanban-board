{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node",
      "config": {
        "npmArgs": "--legacy-peer-deps"
      }
    }
  ],
  "buildCommand": "npm install --legacy-peer-deps && next build"
}